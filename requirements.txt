# 部署用依赖（已锁定版本，保证云端与你本地跑通的一致）
# 本地开发若想升级，先在本机 pip install 验证后再改这里。
fastapi==0.139.0
uvicorn[standard]==0.50.2
openai==2.44.0
python-dotenv==1.2.2
httpx==0.28.1
langgraph==1.2.8
langchain-openai==1.3.3
langchain-core==1.4.8
langgraph-checkpoint==4.1.1
mcp==1.28.1
langchain-mcp-adapters==0.3.0

# ===== 文档理解引擎依赖（多格式解析 + 跨文档检索）=====
# 这些库在 documents.py 内为「懒加载」，缺一个只会让对应格式降级，不会让启动崩。
# 但要在云端完整支持上传/解析，必须全部安装。
pypdf==6.14.2
pdfplumber==0.11.10
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
pandas==3.0.3
beautifulsoup4==4.15.0
trafilatura==2.1.0
pillow==12.3.0
rank-bm25==0.2.2
lxml==6.1.1

# 可选：扫描件/图片 OCR（云端需额外安装系统级 tesseract 二进制，否则自动降级）
# pytesseract==0.3.13
# pdf2image==1.17.4

